trigger SalesDatabase_tgr on Sales_Database__c (before insert,after insert,  before update, after update) {
  // Removed commented unused code which was replaced by the other Sales Database trigger in use ( or active right now ) for clarity. 
}